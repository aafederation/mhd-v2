{
  "name": "starter-basic",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tinacms dev -c \"next dev -p 1313\"",
    "build": "tinacms build && next build",
    "build-n": "next build",
    "start": "tinacms build && next start",
    "lint": "next lint"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "next": "^14.2.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "tinacms": "^2.1.0",
    "toml": "^3.0.0"
  },
  "devDependencies": {
    "@tinacms/cli": "^1.5.34",
    "eslint": "7",
    "eslint-config-next": "12.0.3"
  }
}
